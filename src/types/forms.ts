export type PersonalFormValues = {
  avatar: string;
  name: string;
  email: string;
  dob: string;
  tel: string;
};

export type BusinessFormValues = {
  name: string;
  address: string;
  city: string;
  county: string;
  code: string;
  country: string;
  taxNumber: string;
  tel: string;
};
